"use client";

import {
    Anime,
    Friends,
    FriendShips,
    GroupMembers,
    Groups,
    PageProps,
} from "@/types";

export default function GroupPassword({
    auth,
    anime,
    season_id,
    episode_id,
    group,
}: PageProps<{
    anime: Anime;
    season_id: number;
    episode_id: number;
    group: Groups;
}>) {
    return <>123</>;
}
